<div class="news-item type-3" data-pageNum="{{pageNum}}" data-index="{{index}}">
  <div class="main">
    <h1>{{ title }}</h1>
    <div class="imgs">
      <img src="{{thumbnail_pic_s}}"/>
      <img src="{{thumbnail_pic_s02}}"/>
      <img src="{{thumbnail_pic_s03}}"/>
    </div>
  </div>
  <div class="info">
    <div class="author">{{author_name}}</div>
    <div class="date">{{date}}</div>
  </div>
</div>